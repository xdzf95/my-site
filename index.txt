<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AI 个人网站</title>
<style>
  *{
    margin:0;
    padding:0;
    box-sizing:border-box;
    font-family: -apple-system, BlinkMacSystemFont, sans-serif;
  }
  body{
    background:#0b0f19;
    color:#fff;
  }

  /* 登录界面 */
  .login-page{
    display:flex;
    align-items:center;
    justify-content:center;
    min-height:100vh;
    padding:20px;
  }
  .login-box{
    background:rgba(255,255,255,0.08);
    backdrop-filter:blur(20px);
    padding:40px 30px;
    border-radius:20px;
    width:100%;
    max-width:400px;
    border:1px solid rgba(255,255,255,0.2);
  }
  .login-box h2{
    text-align:center;
    margin-bottom:24px;
  }
  input{
    width:100%;
    padding:14px 16px;
    margin:8px 0;
    border-radius:12px;
    border:none;
    outline:none;
    background:rgba(255,255,255,0.1);
    color:#fff;
    font-size:16px;
  }
  button{
    width:100%;
    padding:14px;
    border-radius:12px;
    border:none;
    background:#007bff;
    color:#fff;
    font-size:16px;
    margin-top:8px;
    cursor:pointer;
    transition:0.2s;
  }
  button:hover{
    background:#0066dd;
  }
  button:active{
    transform:scale(0.97);
  }

  /* 主内容（登录后显示） */
  .main-page{
    display:none;
    padding:20px;
    max-width:800px;
    margin:0 auto;
  }
  .title{
    text-align:center;
    margin:40px 0;
  }
  .chat-box{
    height:500px;
    overflow-y:auto;
    background:rgba(255,255,255,0.05);
    border-radius:16px;
    padding:16px;
    margin-bottom:16px;
  }
  .msg{
    padding:10px 14px;
    border-radius:12px;
    margin:8px 0;
    max-width:75%;
  }
  .user{
    background:#007bff;
    margin-left:auto;
  }
  .ai{
    background:#222;
    margin-right:auto;
  }
  .input-row{
    display:flex;
    gap:10px;
  }
</style>
</head>

<body>

<!-- 登录 -->
<div class="login-page" id="loginPage">
  <div class="login-box">
    <h2>管理员登录</h2>
    <input type="text" id="username" placeholder="账号">
    <input type="password" id="password" placeholder="密码">
    <button onclick="login()">登录</button>
  </div>
</div>

<!-- 主网站（带AI） -->
<div class="main-page" id="mainPage">
  <div class="title">
    <h1>AI 智能助手</h1>
  </div>
  <div class="chat-box" id="chatBox"></div>
  <div class="input-row">
    <input type="text" id="msg" placeholder="输入问题..." style="flex:1;">
    <button onclick="sendMsg()" style="width:auto;padding:0 20px;">发送</button>
  </div>
</div>

<script>
// ———————————— 你自己设置账号密码 ————————————
const MY_USER = "admin"
const MY_PWD  = "123456"

// 登录
function login(){
  let u = document.getElementById("username").value
  let p = document.getElementById("password").value
  if(u === MY_USER && p === MY_PWD){
    alert("登录成功")
    document.getElementById("loginPage").style.display = "none"
    document.getElementById("mainPage").style.display = "block"
  }else{
    alert("账号或密码错误")
  }
}

// AI 聊天交互
async function sendMsg(){
  let msg = document.getElementById("msg").value.trim()
  if(!msg) return

  let chat = document.getElementById("chatBox")

  // 用户消息
  let userDiv = document.createElement("div")
  userDiv.className = "msg user"
  userDiv.textContent = msg
  chat.appendChild(userDiv)

  // 清空输入
  document.getElementById("msg").value = ""

  // AI 回复
  let aiDiv = document.createElement("div")
  aiDiv.className = "msg ai"
  aiDiv.textContent = "思考中..."
  chat.appendChild(aiDiv)
  chat.scrollTop = chat.scrollHeight

  // 模拟AI回复
  setTimeout(()=>{
    aiDiv.textContent = "你好！我是你的AI助手，有什么我可以帮助你的吗？"
    chat.scrollTop = chat.scrollHeight
  },800)
}
</script>

</body>
</html>